# Development Log — 2026-04-10

## 03:00 - Editor UX fixes (fix/editor-ux-fixes)

**Purpose:** Fix UX issues: heavy iframe thumbnails, missing metadata editing, missing media management, broken image paths, and media file management features.

- [ID-050] Reverted card thumbnails from iframe-based SlideThumbnail (N API calls, bad fit) to deterministic gradient. Removed SlideThumbnail component entirely
- [ID-051] Added Config tab (4th tab) to editor with MetadataForm component: title, subtitle, author, theme fields with change detection and PATCH API save
- [ID-052] Added Media tab (5th tab) to editor with MediaPanel component: drag & drop upload, file listing with thumbnails, copy markdown reference button, delete. API routes for list/upload/delete/serve media files
- [ID-053] Created rewriteMediaPaths() utility to convert relative image paths (./photo.png) to API endpoints in Marp render output. Works in both editor preview and presentation mode. 16 unit + integration tests
- [ID-054] Added rename (inline edit with PATCH API) and large preview popup (overlay with full-size image, close button, copy markdown ref) to MediaPanel. 4 new tests
- [ID-055] Added default CSS for auto-fit images: flexbox layout on sections, images flex-grow with object-fit:contain to fill remaining slide space
- [ID-056] Wired CSS/JS tabs to Marp rendering pipeline: MarpRenderer port now accepts JS param, renderer injects as script block, render API passes js, editor sends js in all preview calls. JS tab now triggers live preview re-render
- [ID-057] Wrote comprehensive user documentation covering: creating presentations, editing slides, images/media management, custom CSS (with examples), custom JavaScript (with examples), presenting, managing presentations
- Updated CLAUDE.md with workflow execution protocol (always create tasks first, commit per task, no auth needed for branch commits)
- EditorTabs now supports 5 tabs: Markdown, CSS, JS, Media, Config
- Total: 161 tests passing across 23 files

## 02:00 - Editor page (feat/editor-page)

**Purpose:** Build the full Editor page with CodeMirror 6 code editor, live Marp preview, and API wiring for load/save.

- Installed CodeMirror 6 packages: codemirror, lang-markdown, lang-css, lang-javascript, theme-one-dark, state, view, language
- Built CodeEditor component: CodeMirror 6 wrapper with oneDark theme, syntax highlighting for markdown/CSS/JS, external value sync, onChange callback
- Built EditorTabs component: Markdown / CSS / JS tab bar with icons (FileText, Palette, Braces), active state with accent border, accessible (tablist/tab/aria-selected)
- Built PreviewPanel component: header with Preview label + slide counter (monospace) + Present button (gradient), 16:9 slide area with SlideRenderer, navigation dots for slide switching
- Wired full Editor page: loads presentation from API on mount, CodeMirror edits with tab switching, debounced live Marp preview (500ms), debounced auto-save to API (1s), side-by-side 50/50 layout, Present button navigates to /present/:id
- Fixed React 19 TypeScript issue with useRef requiring initial value
- Added 21 new tests: CodeEditor (4), EditorTabs (4), PreviewPanel (6), EditorPage (7) — total 109 tests passing

## 01:00 - Home page (feat/home-page)

**Purpose:** Build the Home page with presentation card grid, create new presentation flow, and delete with confirmation dialog.

- Built PresentationCard component: landscape layout with gradient thumbnail (~40%), info panel (~60%) with title, description, date, action icons (edit, delete, present, details). Deterministic gradient colors per presentation ID
- Built ConfirmDialog component: modal overlay with title, message, confirm/cancel buttons, destructive mode (red confirm button)
- Rewrote Home page as client component consuming the presentations API: fetches summaries on mount, renders 3-column card grid sorted by last modified
- Implemented "+ New Presentation" flow: creates via POST API, redirects to editor
- Implemented delete flow: click trash icon → ConfirmDialog → DELETE API → refresh list
- Empty state message when no presentations exist
- Added 25 new tests: PresentationCard (10), ConfirmDialog (6), HomePage (9) — total 84 tests passing

## 00:45 - Marp rendering + Presentation page (feat/marp-rendering)

**Purpose:** Integrate Marp Core rendering engine and build the complete Presentation page for fullscreen slide viewing with navigation.

- Defined MarpRenderer port interface in `core/ports/` with render() returning HTML, slideCount, and CSS
- Implemented MarpCoreRenderer adapter using `@marp-team/marp-core` — converts Marp markdown to complete HTML documents with embedded styles
- Created SlideRenderer UI component — sandboxed iframe rendering with srcdoc, slide navigation by index, slide count reporting
- Created POST `/api/presentations/:id/render` route — renders stored presentation or accepts raw markdown for live preview
- Built complete Presentation page (`/present/:id`) matching the design:
  - 16:9 centered slide area with shadow on dark background
  - Floating top control bar (semi-transparent, blurred) with back button, slide counter (monospace), fullscreen/grid/settings icons
  - Left/right chevron navigation arrows (circular, semi-transparent)
  - Full keyboard navigation: arrows, space (next), escape (back/exit fullscreen), F (fullscreen toggle)
- Added 11 new tests (Marp renderer: 6, SlideRenderer: 5) — total 56 tests passing

## 22:10 - Data foundation (feat/data-foundation)

**Purpose:** Build the complete data layer — domain model, storage port, filesystem adapter, seed data, and REST API — so all pages have a backend to consume.

- Defined presentation domain model in `core/domain/presentation.ts`: entities (Presentation, PresentationSummary), value objects (HistoryEntry, ConversationMessage, PresentationMetadata), factory functions (createPresentation, countSlides, toSummary)
- Defined PresentationRepository port interface in `core/ports/` with list, getById, create, updateContent, updateMetadata, delete, exists methods
- Implemented FilesystemPresentationRepository adapter in `infrastructure/storage/` — reads/writes presentation folders with 6 files each (presentation.md, styles.css, scripts.js, metadata.json, history.json, conversation.json)
- Created repository singleton factory using STORAGE_PATH env var
- Created seed script (`scripts/seed-data.ts` + `scripts/seed.sh`) with 3 sample Marp presentations (Building Modern APIs, Design System v2, Engineering Onboarding)
- Created REST API routes: GET/POST `/api/presentations`, GET/PUT/PATCH/DELETE `/api/presentations/:id`
- Switched docker-compose from named volume to local `data/presentations/` bind mount
- Fixed port mapping in docker-compose (3737 external → 3000 internal)
- Added tsx dev dependency for running seed scripts
- Added 30 new tests: domain model (12), filesystem adapter (18) — total 45 tests passing

## 01:50 - Walking skeleton (skeleton/walking-skeleton)

**Purpose:** Establish the complete project foundation — Docker-first development environment, hexagonal architecture, test infrastructure, automation scripts, and empty page shells for all 5 routes.

- Initialized git repository on `skeleton/walking-skeleton` branch
- Created project folder structure following hexagonal architecture (core/ports, core/domain, infrastructure, ui/components)
- Set up Next.js 15 with App Router, TypeScript, React 19
- Configured Tailwind CSS v4 with all design tokens from the design spec (surfaces, foreground, accents, borders, radii)
- Integrated fonts: Inter (headings), Geist Sans (body), Geist Mono (code), Funnel Sans (captions)
- Created logger abstraction (`ConsoleLogger` implementing `Logger` interface) with factory function
- Built shared `TopBar` component with Lucide icons, active route highlighting, props-driven design
- Created 5 empty page shells: Home (`/`), Editor (`/editor/:id`), Present (`/present/:id`), Details (`/details/:id`), Settings (`/settings`)
- Set up Docker multi-stage build (dev + prod targets) with Node 22 Alpine + pnpm
- Created `docker-compose.yml` with hot reload (bind mounts), env vars from `.env`, healthcheck
- Created `.env` / `.env.example` with all configurable parameters (ports, AI config, storage path)
- Created 6 automation scripts: `dev.sh`, `run.sh`, `build.sh`, `test.sh`, `commit.sh`, `push.sh`
- Configured Vitest with happy-dom, React Testing Library, path aliases
- Wrote tests: logger (6 tests), TopBar (4 tests), HomePage (2 tests), SettingsPage (2 tests)
- Created 3 ADRs: hexagonal architecture, Docker-first dev, Vitest
- Created C4 diagrams (model.dsl + views.dsl) with system context, container, and component views
- Set up docs scaffolding: tasks (backlog, done, id tracking), devlog, user guide, ADRs, diagrams
