# Development Log — 2026-04-14

## 02:00 - Mermaid + Chart.js rendering plugins (feat/rendering-plugins)

**Purpose:** Add Mermaid.js diagrams and Chart.js charts as rendering plugins in Marp slides.

- [ID-059] Mermaid.js: bundle (3MB) in public/ for offline. Injected into rendered HTML. Auto-renders ```mermaid code blocks → SVG. Dark theme. Proportional fit via CSS flex + max-width/height
- [ID-060] Chart.js: UMD bundle (204KB) in public/. Parses ```chart JSON blocks → canvas charts. Dark-themed defaults. All Chart.js types supported
- Both work in editor preview and presentation mode
- User docs updated with syntax examples
- 236 tests passing

## 01:00 - Export functionality (feat/export)

**Purpose:** Implement all three export formats: PDF, PPTX, and Markdown.

- [ID-046] Markdown export was already implemented (downloads .md file from Details action bar)
- [ID-047] PDF export: opens rendered Marp HTML in a new window with print-optimized CSS (@page 1280x720), triggers window.print(). Works fully offline — user selects "Save as PDF" in the browser print dialog
- [ID-048] PPTX export: uses pptxgenjs via dynamic import (Function() trick to bypass webpack static analysis). Parses Marp markdown into slide content blocks, creates 16:9 PPTX with dark theme. Handles headings, bullet points, images (as text placeholders)
- [ID-049] Wired all export buttons in Details page action bar: PDF (new window print), PPTX (download), Markdown (download)
- Export utilities organized in src/ui/utils/ (export-pdf, export-pptx, export-markdown)
- Total: 236 tests passing across 32 files
