# Development Log — 2026-04-12

## 09:00 - Offline, home polish, filtering (fix/offline-and-home-polish)

**Purpose:** Make app fully offline for plane use, auto-checkpoint properties, redesign cards, add filtering.

- [ID-079] Offline: stripped CDN @import from Marp CSS, bundled Lato + Roboto Mono as local woff2 via @fontsource. Test verifies no CDN URLs. App fonts (Inter/Geist/Funnel Sans) already offline via next/font
- [ID-080] Auto-checkpoint: metadata save (Config tab) now creates a "Properties changed" version snapshot
- [ID-081] Home cards: compact horizontal layout with 20px color strip, inline title/description/slide count/date + action icons. List layout instead of 3-column grid
- [ID-082] Real-time filtering: search bar with field selector (Title/Subtitle/Author), client-side filtering via useMemo, list reduces as user types. Author field added to PresentationSummary
- Total: 236 tests passing across 32 files

## 07:00 - Navigation & real theme system (feat/navigation-and-themes)

**Purpose:** Add back button, clickable titles, and rework the theme system to use real CSS files compatible with Marp engine.

- [ID-070] Back button in TopBar (arrow-left, visible on all pages except Home, uses router.back())
- [ID-071] Clickable title in Home cards (cursor:pointer, hover:underline, opens editor)
- [ID-072] Theme model reworked: themes are real CSS files in data/presentations/themes/. Built-in themes (default/gaia/uncover) listed alongside custom. API: GET/POST /api/themes, GET/PUT/DELETE /api/themes/:id. Slugified names enforced.
- [ID-073] MarpCoreRenderer loads custom theme CSS files from themes/ folder and registers them via marp.themeSet.add() before rendering. Custom themes are now usable via `theme: my-theme` in frontmatter.
- [ID-074] Theme CSS editor in Settings: list all themes (built-in locked, custom editable), create new (slugified name), edit CSS with CodeMirror, save/delete. CSS must include `@theme` directive.
- [ID-075] Updated wording: "Available Themes" with explanation that themes are system-wide and referenced by name in frontmatter.
- Total: 233 tests passing

## 06:00 - Configuration page (feat/configuration-page)

**Purpose:** Build the full Configuration/Settings page with sidebar navigation, theme gallery, plugin toggles, editor preferences, and filesystem persistence.

- [ID-041] Settings sidebar: 5 sections (Themes, Plugins, Editor, Export, About) with icons, active state highlighting, click-to-scroll
- [ID-042] Theme gallery: 6 themes in 3-column grid (Default Dark, Minimal Light, Neon Glow, Corporate Blue, Nature Green, Retro Amber), gradient previews, active theme has accent border
- [ID-043] Plugin toggle cards: KaTeX, Mermaid, Syntax Highlighting, Speaker Notes with animated toggles, descriptions, icons
- [ID-044] Editor preferences: font size (10-24) and tab size (1-8) number steppers, auto-save and line numbers toggles
- [ID-045] Persistence: settings domain model (AppSettings, ThemeConfig, PluginConfig, EditorPreferences), GET/PUT /api/settings reading/writing settings.json in storage root
- About section with version info
- 5 page tests, total 233 passing

## 05:00 - Editor UX round 2 (fix/editor-ux-round2)

**Purpose:** Fix 5 UX issues: conversation persistence, rolling context, cross-navigation, resizable panels, and scroll sync.

- [ID-067] Fixed conversation persistence: saveConversation in port+adapter, GET/PUT API route, auto-save after AI exchange via useEffect, conversation now visible in Details page
- [ID-068] Rolling context window: trimHistory() keeps most recent messages under ~4K tokens (16K chars). Clear button (eraser icon) in assistant panel header
- [ID-069] Editor ↔ Details navigation: context bar in editor with title + Details link, Edit button (gradient) in Details action bar
- [ID-065] Resizable panels: ResizablePanels component with draggable divider (mousedown/mousemove), min/max constraints, double-click reset. Wired into editor for code/preview split
- [ID-066] Synchronized scroll: CodeEditor tracks cursor slide position by counting --- separators before cursor, emits onCursorSlide. PreviewPanel accepts external currentSlide prop. Preview follows editor cursor position
- Total: 230 tests passing across 31 files

## 04:00 - AI diff review (feat/ai-integration continued)

**Purpose:** Replace silent markdown replacement with a visual diff review — like VS Code inline diff with per-chunk accept/reject.

- [ID-063] Built DiffEditor component using @codemirror/merge unifiedMergeView: inline diff with per-chunk Accept/Reject buttons (built-in), Accept All / Reject All / Done action bar, dark theme for insertions (green) and deletions (red)
- Changed AI response flow: pendingDiff state → DiffEditor replaces CodeEditor → user reviews → on resolve, final content applied + preview + save
- Updated system prompt to mention diff review
- 4 new tests, total 227 passing

## 03:00 - AI integration (feat/ai-integration)

**Purpose:** Wire the AI assistant end-to-end: port interface → OpenAI-compatible adapter → streaming API route → editor panel with live token display and markdown-to-editor bridge.

- [ID-034] Defined AIAssistant port: chat() and chatStream() methods, AIAssistantConfig, ChatRequest/ChatResponse types
- [ID-035] Implemented OpenAICompatibleAssistant: fetch-based adapter for /v1/chat/completions, streaming via SSE parsing, markdown extraction from fenced code blocks, configurable via env vars. Default system prompt with Marp syntax reference. 8 tests
- [ID-036] Created POST /api/presentations/:id/chat route: accepts message+history+markdown, streams AI response as SSE events, final event includes full content + extracted markdown update
- [ID-037] Wired AssistantPanel to the chat API: user messages sent via fetch, SSE stream consumed token-by-token, assistant bubble updated live, loading state while streaming, error messages on failure
- [ID-038] AI-to-editor bridge: when AI response contains a ```markdown code block, the markdown is extracted and applied directly to CodeMirror state + preview re-render + auto-save
- Total: 223 tests passing across 29 files

## 02:00 - Change history + Details page (feat/change-history)

**Purpose:** Build the full change history system and the Details page with 3-column layout.

- [ID-027] Extended HistoryEntry with optional description field, added createHistoryEntry() factory. 4 tests
- [ID-028] Added addHistoryEntry/getHistory to port and filesystem adapter. updateContent now auto-logs history entries (tracks which files changed). 7 tests
- [ID-029] Created GET /api/presentations/:id/history API route
- [ID-030] Built MarkdownViewer: read-only, line-numbered, monospace with syntax coloring. 4 tests
- [ID-031] Built HistoryTimeline: vertical timeline with color-coded dots, reverse chronological. 6 tests
- [ID-032] Built Details page 3-column layout: markdown source | change history | AI conversation log
- [ID-033] Built Details action bar: back arrow, title, slide count badge, export buttons (Markdown download working, PDF/PPTX placeholders). 9 tests
- Total: 215 tests passing across 28 files

## 01:00 - AI assistant panel UI (feat/ai-assistant-panel)

**Purpose:** Build the collapsible AI assistant panel UI shell in the editor, without actual AI wiring.

- [ID-024] Built ChatBubble component: user/assistant variants with role-based styling (elevated bg for AI, dark bg for user), accent label, optional timestamp, data-role attribute. 6 tests
- [ID-025] Built AssistantPanel component: header (sparkles + title + close), scrollable chat area with auto-scroll, empty state prompt, text input with Enter-to-send, send button disabled when empty, loading indicator ("Thinking..." with pulsing dot), relative timestamp formatting. 11 tests
- [ID-026] Wired assistant panel into editor page: sparkles toggle button in tab bar, collapsible 20/40/40 layout (assistant/code/preview) vs 50/50 when closed, loads conversation from presentation data, placeholder sendMessage handler (ready for AI API). Loads existing conversation on mount
- Total: 187 tests passing across 26 files
