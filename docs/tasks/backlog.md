# Backlog



## Future — Search

- [FEAT : ID-083] ? Full-text search across presentation content (requires indexing service or file-based search)

## Future — Rendering Plugins (? — confirm with user before starting)

- [FEAT : ID-058] ? Add LaTeX/KaTeX notation support as a slide rendering plugin

## Future — Rendering Engine Abstraction (? — confirm with user before starting)

- [STRUCT : ID-061] ? Abstract slide rendering engine behind a port so Marp can be swapped for Reveal.js or other engines
